apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-db
  labels:
    app: event-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-db
  template:
    metadata:
      labels:
        app: event-db
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: event-service-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: event-service-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: event-service-secret
                  key: POSTGRES_DB
          volumeMounts:
            - name: postgres-storage-event
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage-event
          persistentVolumeClaim:
            claimName: postgres-pvc-event
